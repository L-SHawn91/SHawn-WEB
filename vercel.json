{
    "buildCommand": "npm run build",
    "framework": "nextjs",
    "installCommand": "npm install",
    "functions": {
        "app/api/papers/search-parallel/route.ts": { "maxDuration": 60 },
        "app/api/datasets/search/route.ts": { "maxDuration": 60 }
    }
}