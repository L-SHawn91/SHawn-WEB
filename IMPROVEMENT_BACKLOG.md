# Improvement Backlog

## P0
- Enforce PR build gate (`lint` + `build`) as required check on `main`.
- Add papers page/API E2E smoke test in CI.

## P1
- Add route-level health checks for critical pages and APIs.
- Add deployment rollback playbook in repository docs.

## P2
- Add Lighthouse baseline budget for key pages.
- Add visual regression checks for dashboard UI blocks.
